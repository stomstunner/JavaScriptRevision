// loop on array 
let arr = ["Superman", "Batman", "Ironman", "Spiderman", "Antman"]
for(let i = 0;i<arr.length;i++){
    console.log(arr[i]);
    // we go out of bound of  the array so at last we get a undefined value
    // so we run loop from 0 to n-1
}